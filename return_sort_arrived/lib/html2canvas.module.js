// lib/html2canvas.module.js
// Chỉ định nghĩa export từ global sau khi script được load
// Giả sử html2canvas.min.js đã được load trước đó (qua content script hoặc injected)
export default window.html2canvas;